# Base URL for the real backend API (unused while VITE_USE_MOCKS=true)
VITE_API_BASE_URL=http://localhost:8000/api

# "true" (default when unset) serves every service call from local mock data.
# Set to "false" once the backend in ../backend is ready to switch over.
VITE_USE_MOCKS=true

# PostHog project API key + region host (client-side key, safe to expose in the build)
VITE_PUBLIC_POSTHOG_KEY=phc_xxxxxxxxxxxxxxxxx
VITE_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Dashboard region-map basemap. Left unset, the map uses keyless OpenStreetMap
# tiles. Point these at a keyed provider (for example a neutral CARTO or
# MapTiler style, key included in the URL) to override the basemap and its
# attribution. The {z}/{x}/{y} placeholders are required.
# VITE_MAP_TILE_URL=https://tile.example.com/{z}/{x}/{y}.png?key=xxxxxxxx
# VITE_MAP_TILE_ATTRIBUTION=&copy; Example Maps
